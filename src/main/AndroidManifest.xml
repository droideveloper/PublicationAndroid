<manifest
  package="org.fs.publication"
  xmlns:android="http://schemas.android.com/apk/res/android">

  <application android:theme="@style/LibraryTheme">

    <activity
      android:name=".views.ReadActivity"
      android:screenOrientation="portrait">

      <intent-filter>

        <action android:name="android.intent.action.VIEW" />
        <category android:name="org.fs.category.READ_MAGAZINE" />

      </intent-filter>

    </activity>

  </application>

</manifest>
